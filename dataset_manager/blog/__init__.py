"""Posts written in WordPress, rendered by this site.

WordPress is the editor and nothing else: it is never publicly served, and
calories.jp/blog is rendered from these tables by the same templates as every
other page. That keeps one design system, one sitemap, one cache policy and one
public attack surface — WordPress being the most attacked software on the web is
not a reason to avoid using it, but it is a reason not to put it on the domain.
"""
